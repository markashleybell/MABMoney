@model MABMoney.Web.Models.Transactions.IndexViewModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Master.cshtml";
}

<h2>Transactions</h2>

<table class="table table-striped">
    <caption>Transaction List</caption>
    <thead>
        <tr>
            <th>Date</th>
            <th>Account</th>
            <th>Category</th>
            <th>Description</th>
            <th>Amount</th>
            <th>Edit</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var transaction in Model.Transactions)
        {
        <tr>
            <td>@transaction.Date.ToString("dd/MM/yyyy")</td>
            <td>@transaction.Account.Name</td>
            <td>@transaction.Category.Name</td>
            <td>@transaction.Description</td>
            <td>@transaction.Amount.ToString("#0.00")</td>
            <td>@Html.ActionLink("Edit", "Edit", new { id = transaction.TransactionID })</td>
        </tr>
        }
    </tbody>
</table>

<div class="row button-row">
    <div class="span2">
        @Html.ActionLink("Add New Transaction", "Create", null, new { @class = "btn btn-primary" })
    </div>
    <div class="span8">&nbsp;</div>
    <div class="span2">
        @Html.ActionLink("Import Transactions", "Import", null, new { @class = "btn btn-primary pull-right" })
    </div>
</div>
